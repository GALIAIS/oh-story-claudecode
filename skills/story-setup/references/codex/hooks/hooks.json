{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "for PYBIN in python3 python py; do \"$PYBIN\" -c \"\" 2>/dev/null && break; done; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-}}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" session-start",
            "timeout": 10,
            "statusMessage": "Loading story context"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|apply_patch|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for PYBIN in python3 python py; do \"$PYBIN\" -c \"\" 2>/dev/null && break; done; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-}}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" pre-tool-prose-guard",
            "timeout": 10,
            "statusMessage": "Checking story outline guard"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "for PYBIN in python3 python py; do \"$PYBIN\" -c \"\" 2>/dev/null && break; done; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-}}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" pre-tool-commit-advisory",
            "timeout": 15,
            "statusMessage": "Checking story commit warnings"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "for PYBIN in python3 python py; do \"$PYBIN\" -c \"\" 2>/dev/null && break; done; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-}}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" pre-compact",
            "timeout": 10,
            "statusMessage": "Summarizing story context"
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "for PYBIN in python3 python py; do \"$PYBIN\" -c \"\" 2>/dev/null && break; done; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-}}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" post-compact",
            "timeout": 10,
            "statusMessage": "Restoring story context hint"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for PYBIN in python3 python py; do \"$PYBIN\" -c \"\" 2>/dev/null && break; done; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-}}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" stop",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
